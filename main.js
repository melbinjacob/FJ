function scrollToFooter() {
    var footer = document.getElementById("footer"); // Get the footer element by its ID
    footer.scrollIntoView({ behavior: 'smooth' }); // Scroll to the footer element with smooth animation
}
  